name: Test Discord

on:
  push:
    branches:
      - master  

jobs:
  enviar-mensaje:
    runs-on: ubuntu-latest

    steps:
      - name: Ejecutar go test y guardar resultado
        id: paso1
        run: |
          go test ./... > paso1_output.txt 2>&1 || true
          cat paso1_output.txt

      - name: Guardar archivo como artefacto
        if: always()
        uses: actions/upload-artifact@v2
        with:
          name: all_steps_output
          path: paso1_output.txt

      - name: Descargar artefacto
        if: always()
        uses: actions/download-artifact@v2
        with:
          name: all_steps_output

      - name: Enviar archivo a Discord
        if: always()
        run: |
          curl -X POST -H 'Content-Type: application/octet-stream' --data-binary @paso1_output.txt ${{ secrets.DISCORD_WEBHOOK }}
