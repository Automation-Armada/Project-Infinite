name: Test Discord

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  Test_Discord:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v2
      - name: slack  
        uses: sarisia/actions-status-discord@v1
      - run: |
         curl -X POST -H 'Content-type: application/json' --data '{"text":"Hello, World!"}'https://hooks.slack.com/services/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
